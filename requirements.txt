# Nova Agent Dependencies
# Core requirements for running the Nova agent

# Required
numpy>=1.24.0
requests>=2.28.0
python-dotenv>=1.0.0

# Optional: Voice (uncomment if needed)
# sounddevice>=0.4.0

# Optional: Vector DB (uncomment if needed)
# chromadb>=0.4.0

# Optional: ML (uncomment if needed - large download)
# torch>=2.0.0
